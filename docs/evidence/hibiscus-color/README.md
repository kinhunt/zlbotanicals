# Hibiscus endpoint article — integration evidence

Base: b594be14c68112974524ad090eaba1e5c63e6f4d (fresh origin/main, 2026-09-22). Research-only EN/ZH blog; no product route or offer. Original public tree and dependencies unchanged.

## Evidence and approved text

`approved-revision/` is a byte-identical copy of all 14 accepted revision artifacts, including both primary XML captures, extracted text, ledger, verdict, source-cell verification and exact approved prose. Original discovery package (41 files) remains unchanged. Source IDs remain 2 (2014) and 3 (2015); unused background ID1 is not published.

`owner-authorized-delta.json` records the subsequent bounded parent authorization: reader-focused EN/ZH heading and removal of review-diary wording in the small-difference paragraph. All other prose is preserved; citation anchors, source links, Chinese 来源, table markup and metadata are integration changes. Regression tests compare every approved paragraph/heading/cell after these authorized substitutions.

`integration-verification.json` compares the 15 displayed mean ± terms per language to original XML Table4 40R rows. ± terms are undefined, not SD/SE. Identical before-storage rows are combined, never averaged. Source's 40R footnote error is retained as an explicit caveat. No retention chart, pairwise significance, powder dosage, health claim or production recipe is inferred.

## Per-table presentation decision — independent judgment REQUIRED next

There is ONE substantive table in each language: five package/storage conditions × L*, a*, b*. Desktop is a semantic descriptive comparison table, not a chart; at <=600px each condition becomes a whole-row card with repeated coordinate labels. All 15 values and condition labels remain in a single DOM, including no-JS. This representation retains identity and undefined ± terms rather than implying a statistical error-bar plot. Desktop permits side-by-side comparison; mobile trades simultaneous cross-condition scanning for complete condition context without horizontal scrolling.

Author checks: EN/ZH 390/1440/320px × JS on/off, 12/12. Each case checks all cell bounds, >=16px cell text, >200px caption width, card/table mode, 2 native keyboard citations, and 4 inbound journeys (research, beverages, application guides, blog). No page errors or non-GET requests. Author vision inspected both desktop tables, mobile card captures and actual320px viewports. Tall element screenshots place the fixed site header across the second card title; actual card-positioned viewport captures show the full title and all values below the header. This is not missing content or an overflow fix.

Independent reviewer must explicitly judge BOTH localized desktop tables AND mobile cards (including320px), not merely accept overflow or keyboard metrics. Confirm chart/table choice, row context, all 15 exact values, undefined ± caveat, mobile caption and condition-label readability, and authorized prose delta. No independent acceptance is claimed here.

## Author verification and remaining gates

Baseline npm test298/298; final npm test304/304,222HTML pages. Initial failing regression receipts and stale inventory/source-preservation assertion failures retained outside repository. Updated historical tests exclude only the new article pair/additive beverage section while preserving previous hash/source comparisons. No dependency changes: npm ci still reports14 vulnerabilities (1critical,11high,1moderate,1low) and existing install-script-policy warnings; not remediated by this editorial release.

Frozen dist, complete manifests, browser results, screenshots, logs and handoff: `/data/hermes/research/seo-growth/2026-09-22-hibiscus-release/`. Review against `frozen-dist`, not a concurrently rebuilt worktree dist. No commit, push, PR, merge or deployment performed. Parent owns independent review and later publication authorization.
