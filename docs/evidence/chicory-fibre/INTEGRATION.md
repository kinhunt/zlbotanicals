# Chicory fibre integration

Base: PR56 `6a15ea935c4aad848f62dc512ffc5d0c2e843d1b`.

`reviewed/` preserves all eight independently reviewed artifacts byte-for-byte; `original/` preserves all 44 discovery artifacts, including unused discovery-only IDs and raw captures. Source IDs 1,2,3,7,8,9 keep their exact destinations, with article-local `chicory-ref-N` targets. Chinese bibliography heading/titles are localized; this is presentation-only, not new evidence.

Published body changes are frontmatter/H1 placement, clickable citation markup, and semantic table markup with repeated mobile field labels. The source regression reconstructs the original Markdown table and compares all remaining approved body bytes, and checks every one of fifteen cells. No approved claim wording changes.

The five-row table compares qualitative descriptions/application checks, not numeric performance. At <=640px each complete row becomes one labelled card retaining its identity and both text fields. Percentages and dimensional quotation formula remain prose; unlike mass bases must not become bars or a purity ranking. No dependency changes or brand stock offers.

Editorial acceptance is independent; local author render/test evidence is under `/data/hermes/research/seo-growth/2026-09-22-chicory-release/`. Parent independent implementation/presentation review is still required. No publication has occurred.
