# Evidence-led content release — 2026-09-07

## Scope

- Reworked EN/ZH homepages toward product discovery, supplier qualification and inquiry.
- Replaced unsupported scale and blanket credential claims with product/site-specific documentation review.
- Rewrote 24 product guides, 14 application concepts and 10 buyer articles, preserving all legacy routes.
- Improved procurement inquiry fields, accessible labels and safe URL prefill; preserved FormSubmit recipient and direct-email alternative.
- Added a real bilingual plain-text sourcing checklist, not a fabricated product catalogue or test report.
- Added EN/ZH privacy and website-use notices to resolve existing footer 404s.
- Corrected page-specific canonical and language alternates, added sharing metadata, removed blanket-certification product-directory metadata.
- Added automated content, routing, form, SEO, integrity and browser smoke checks.

## Baseline and local validation

- Previous production/main commit: `55250d9f8592f3bde848ce945f3ec4679912444b`.
- Baseline: `npm ci` and `npm run build` succeeded (92 generated routes).
- After additions: 96 generated pages; `npm test` succeeded, 29 tests passed at initial integrated check.
- Chromium desktop/mobile smoke checks: 14 page/viewport combinations; invalid-input validation, safe prefill, local FormData, mobile menu and browser exceptions checked. Live requests were NOT submitted.
- Independent review and deployment verification must complete before production publication; the PR and deployment records provide final evidence.

## Known limits

- Actual FormSubmit activation and mailbox receipt are not verified. Do not equate validation or navigation with delivered sales email.
- Internal licenses, certification register, actual TDS/COA, production evidence, current commercial terms and approved case studies remain needed. Nothing was fabricated to fill these gaps.
- Existing dependency audit has 14 advisories; major framework migration is deferred to a separate tested release (see content-governance.md).
- Existing product/concept imagery is illustrative, not new photographic proof of facilities or manufactured stock.

## Rollback

Use a new revert commit for the eventual squashed release commit, then push through the verified deployment path. Do not reset or force-push main. Recheck GitHub deployment success and production page content after rollback.
