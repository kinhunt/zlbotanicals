---
import { getCollection } from 'astro:content';
import TeaHazeReader from '../../../components/resources/TeaHazeReader.astro';
import CentellaStabilityReader from '../../../components/resources/CentellaStabilityReader.astro';
import { guideCategories } from '../../../data/resource-guides';
import PageLayout from '../../../layouts/PageLayout.astro';
import { getLocalizedPath } from '../../../i18n/config';
import { Calendar, Tag, ExternalLink, Leaf, Lightbulb } from '@lucide/astro';

export async function getStaticPaths() {
  const posts = await getCollection('blog', ({ data }) => {
    return data.lang === 'en' && !data.draft;
  });

  return posts.map((post) => ({
    params: { slug: post.slug.replace('en/', '') },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content, headings } = await post.render();
const isCentellaStability = post.slug.endsWith('/centella-cold-process-stability');
const isChicory = post.slug.endsWith('/chicory-fibre-selection');
const isVanillaAuthentication = post.slug.endsWith('/vanilla-authentication');
const isVanilla = post.slug.endsWith('/vanilla-material-choice');
const isCranberry = post.slug.endsWith('/cranberry-pac-purchasing');
const isCitrus = post.slug.endsWith('/hesperidin-material-choice');
const isHibiscus = post.slug.endsWith('/hibiscus-color-endpoints');
const isSaffron = post.slug.endsWith('/saffron-identity');
const isTeaHaze = post.slug.endsWith('/tea-haze-diagnosis');
const lang = 'en';

// Get related products
const allProducts = await getCollection('products', ({ data }) => {
  return data.lang === 'en' && !data.draft;
});
const relatedProducts = allProducts.filter((product) =>
  post.data.relatedProducts?.includes(product.slug.replace('en/', ''))
);

// Get related solutions
const allSolutions = await getCollection('solutions', ({ data }) => {
  return data.lang === 'en' && !data.draft;
});
const relatedSolutions = allSolutions.filter((solution) =>
  post.data.relatedSolutions?.includes(solution.slug.replace('en/', ''))
);

const guideCategory = guideCategories.find(category => category.slug === post.data.guideCategory)!;
---

<PageLayout
  title={post.data.title}
  description={post.data.description}
  lang={lang}
  heroTitle={post.data.title}
  heroSubtitle={guideCategory[lang].title}
>
  <article class:list={["section-padding", {"chicory-reader": isChicory, "vanilla-authentication-reader": isVanillaAuthentication, "vanilla-material": isVanilla, "cranberry-pac": isCranberry, "citrus-material": isCitrus, "hibiscus-color": isHibiscus, "saffron-identity": isSaffron, "elderberry-material": post.slug.endsWith("/elderberry-material-selection"), "paprika-format": post.slug.endsWith("/paprika-extract-format-selection"), "sdil": post.slug.endsWith("/uk-sdil-milk-tea-2028"), "ginseng-heat": post.slug.endsWith("/ginseng-heat-bitterness"), "stevia-temporal": post.slug.endsWith("/stevia-temporal-sensory")}]}>
    <div class="container-wide max-w-4xl">
      <!-- Breadcrumb -->
      <nav aria-label="Breadcrumb" class="mb-8 text-sm">
        <ol class:list={["flex items-center gap-2 text-gray-500", {"flex-wrap": isTeaHaze || isCentellaStability}]}>
          <li><a href="/" class="hover:text-primary">Home</a></li>
          <li>/</li>
          <li><a href={getLocalizedPath('/resources', lang)} class="hover:text-primary">Resources</a></li>
          <li aria-hidden="true">/</li>
          <li><a href={getLocalizedPath(`/resources/${guideCategory.slug}`, lang)} class="hover:text-primary">{guideCategory[lang].title}</a></li>
          <li>/</li>
          <li class="text-gray-900 truncate max-w-xs">{post.data.title}</li>
        </ol>
      </nav>

      <!-- Meta -->
      <div class="flex flex-wrap items-center gap-4 mb-8">
        <span class="px-3 py-1 bg-accent/10 text-accent rounded-full text-sm font-medium">
          {guideCategory[lang].title}
        </span>
        <span class="text-gray-500 text-sm flex items-center gap-1">
          <Calendar class="w-4 h-4" />
          {post.data.publishDate}
        </span>
      </div>

      <!-- Tags -->
      <div class="flex flex-wrap gap-2 mb-8">
        {post.data.tags.map((tag) => (
          <span class="px-3 py-1 bg-gray-100 text-gray-600 text-sm rounded-full flex items-center gap-1">
            <Tag class="w-3 h-3" />
            {tag}
          </span>
        ))}
      </div>

      <!-- Feature Image -->
      {post.data.image && (
        <div class="mb-10 rounded-2xl overflow-hidden shadow-lg">
          <img
            src={post.data.image}
            alt={post.data.title}
            class="w-full h-64 md:h-96 object-cover"
          />
        </div>
      )}

      <!-- Main Content -->
      <div class="prose prose-lg max-w-none prose-headings:font-display prose-headings:text-gray-900 prose-a:text-primary prose-a:no-underline hover:prose-a:underline prose-img:rounded-xl prose-table:text-sm">
        {isTeaHaze ? <TeaHazeReader lang={lang} headings={headings}><Content /></TeaHazeReader> : isCentellaStability ? <CentellaStabilityReader lang={lang} headings={headings}><Content /></CentellaStabilityReader> : <Content />}
      </div>

      <!-- Sources -->
      {post.data.sources && post.data.sources.length > 0 && (
        <div class="mt-12 p-6 bg-gray-50 rounded-xl">
          <h3 class="text-lg font-display font-bold text-gray-900 mb-4">Sources</h3>
          <ul class="space-y-2">
            {post.data.sources.map((source, index) => (
              <li class="flex items-start gap-2">
                <span class="text-gray-400">[{index + 1}]</span>
                <a
                  href={source.url}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="text-primary hover:underline flex items-center gap-1"
                >
                  {source.title}
                  <ExternalLink class="w-3 h-3" />
                </a>
              </li>
            ))}
          </ul>
        </div>
      )}

      <!-- Related Products -->
      {relatedProducts.length > 0 && (
        <div class="mt-12 pt-8 border-t border-gray-200">
          <h3 class="text-xl font-display font-bold text-gray-900 mb-6 flex items-center gap-2">
            <Leaf class="w-6 h-6 text-primary" />
            Related Products
          </h3>
          <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-4">
            {relatedProducts.map((product) => (
              <a
                href={`/products/${product.slug.replace('en/', '')}`}
                class="group p-5 bg-off-white rounded-xl hover:shadow-md transition-shadow"
              >
                <h4 class="font-semibold text-gray-900 group-hover:text-primary transition-colors">
                  {product.data.name}
                </h4>
                {product.data.latinName && (
                  <p class="text-xs text-gray-500 italic mt-1">{product.data.latinName}</p>
                )}
              </a>
            ))}
          </div>
        </div>
      )}

      <!-- Related Solutions -->
      {relatedSolutions.length > 0 && (
        <div class="mt-12 pt-8 border-t border-gray-200">
          <h3 class="text-xl font-display font-bold text-gray-900 mb-6 flex items-center gap-2">
            <Lightbulb class="w-6 h-6 text-accent" />
            Related Solutions
          </h3>
          <div class="grid md:grid-cols-2 gap-4">
            {relatedSolutions.map((solution) => (
              <a
                href={`/solutions/${solution.slug.replace('en/', '')}`}
                class="group p-6 bg-gradient-to-br from-accent/5 to-accent/10 rounded-xl hover:shadow-md transition-all"
              >
                <h4 class="font-semibold text-gray-900 group-hover:text-accent transition-colors">
                  {solution.data.name}
                </h4>
                {solution.data.description && (
                  <p class="text-sm text-gray-600 mt-2 line-clamp-2">{solution.data.description}</p>
                )}
              </a>
            ))}
          </div>
        </div>
      )}

      <!-- CTA -->
      {!isChicory && !isSaffron && !isHibiscus && !isCitrus && !isCranberry && !isVanilla && !isVanillaAuthentication && <div class="mt-12 p-8 bg-primary/5 rounded-2xl text-center">
        <h3 class="text-xl font-display font-bold text-gray-900">Interested in our botanical extracts?</h3>
        <p class="text-gray-600 mt-2">Contact us for samples, technical documentation, or custom formulation support.</p>
        <a
          href="/contact"
          class="inline-block mt-4 px-6 py-3 bg-primary text-white rounded-lg font-medium hover:bg-primary-dark transition-colors"
        >
          Get in Touch
        </a>
      </div>}
    </div>
  </article>
</PageLayout>

<style>
  .chicory-reader :global([id]) {scroll-margin-top:100px}
  .chicory-reader :global(nav ol) {flex-wrap:wrap}
  .chicory-reader :global(.prose) {max-width:820px;margin-inline:auto;overflow-wrap:anywhere;line-height:1.85}
  .chicory-reader :global(:focus-visible) {outline:3px solid #a77b16;outline-offset:3px}
  .chicory-reader :global(.chicory-table) {width:100%;table-layout:fixed;border-collapse:collapse}
  .chicory-reader :global(.chicory-table caption) {text-align:left;font-weight:600;padding:1rem 0}
  .chicory-reader :global(.chicory-table th), .chicory-reader :global(.chicory-table td) {padding:1rem;vertical-align:top;border:1px solid #d1d5db;overflow-wrap:anywhere}
  .chicory-reader :global(.mobile-label) {display:none}
  @media(max-width:640px) {
    .chicory-reader :global(.chicory-table), .chicory-reader :global(.chicory-table tbody), .chicory-reader :global(.chicory-table tr), .chicory-reader :global(.chicory-table th), .chicory-reader :global(.chicory-table td) {display:block;width:100%;box-sizing:border-box}
    .chicory-reader :global(.chicory-table caption) {display:block;width:100%}
    .chicory-reader :global(.chicory-table thead) {display:none}
    .chicory-reader :global(.chicory-table tr) {margin-bottom:1.5rem;border:1px solid #d1d5db;border-radius:8px;overflow:hidden}
    .chicory-reader :global(.chicory-table th), .chicory-reader :global(.chicory-table td) {border:0;padding:1rem;font-size:16px;line-height:1.8}
    .chicory-reader :global(.chicory-table th) {background:#f3f4f6}
    .chicory-reader :global(.mobile-label) {display:block;font-weight:600;margin-bottom:.4rem;color:#374151}
  }

  .vanilla-authentication-reader :global([id]) {scroll-margin-top:100px}
  .vanilla-authentication-reader :global(nav ol) {flex-wrap:wrap}
  .vanilla-authentication-reader :global(.prose) {max-width:820px;margin-inline:auto;overflow-wrap:anywhere;line-height:1.85}
  .vanilla-authentication-reader :global(:focus-visible) {outline:3px solid #a77b16;outline-offset:3px}

  .hibiscus-color :global([id]) {scroll-margin-top:100px}
  .hibiscus-color :global(nav ol) {flex-wrap:wrap}
  .hibiscus-color :global(.prose) {max-width:820px;margin-inline:auto;overflow-wrap:anywhere;line-height:1.85}
  .hibiscus-color :global(.hibiscus-table) {width:100%;table-layout:fixed;border-collapse:collapse;margin:1.5rem 0}
  .hibiscus-color :global(caption) {text-align:left;font-weight:600;color:#233e27;margin-bottom:.75rem}
  .hibiscus-color :global(th), .hibiscus-color :global(td) {padding:1rem;text-align:left;vertical-align:top;border:1px solid #d8e1d0;font-size:1rem;line-height:1.6}
  .hibiscus-color :global(th) {background:#eff3e9;color:#233e27}
  .hibiscus-color :global(.mobile-label) {display:none}
  .hibiscus-color :global(:focus-visible) {outline:3px solid #a77b16;outline-offset:3px}
  @media(max-width:600px) {
    .hibiscus-color :global(caption) {display:block;width:100%}
    .hibiscus-color :global(.hibiscus-table), .hibiscus-color :global(tbody) {display:block}
    .hibiscus-color :global(thead) {position:absolute;width:1px;height:1px;overflow:hidden;clip-path:inset(50%)}
    .hibiscus-color :global(tr) {display:block;margin:1rem 0;border:1px solid #d8e1d0;border-radius:.5rem;overflow:hidden}
    .hibiscus-color :global(th[scope="row"]) {display:block;border:0;padding:.85rem 1rem}
    .hibiscus-color :global(td) {display:block;border:0;border-top:1px solid #d8e1d0;padding:.75rem 1rem}
    .hibiscus-color :global(.mobile-label) {display:block;font-size:.9rem;color:#52634e;margin-bottom:.3rem}
  }

  .hibiscus-color :global(.table-value) {white-space:nowrap}
  .hibiscus-color :global(th:first-child) {width:34%}
  @media(max-width:600px) {.hibiscus-color :global(th:first-child) {width:auto}}

  .saffron-identity :global([id]) {scroll-margin-top:100px}
  .saffron-identity :global(nav ol) {flex-wrap:wrap}
  .saffron-identity :global(.prose) {max-width:820px;margin-inline:auto;overflow-wrap:anywhere;line-height:1.85}
  .saffron-identity :global(.saffron-table) {width:100%;table-layout:fixed;border-collapse:collapse;margin:1.5rem 0}
  .saffron-identity :global(caption) {text-align:left;font-weight:600;color:#233e27;margin-bottom:.75rem}
  .saffron-identity :global(th), .saffron-identity :global(td) {padding:1rem;text-align:left;vertical-align:top;border:1px solid #d8e1d0;font-size:1rem;line-height:1.6}
  .saffron-identity :global(th) {background:#eff3e9;color:#233e27}
  .saffron-identity :global(.mobile-label) {display:none}
  .saffron-identity :global(:focus-visible) {outline:3px solid #a77b16;outline-offset:3px}
  @media(max-width:600px) {
    .saffron-identity :global(caption) {display:block;width:100%}
    .saffron-identity :global(.saffron-table), .saffron-identity :global(tbody) {display:block}
    .saffron-identity :global(thead) {position:absolute;width:1px;height:1px;overflow:hidden;clip-path:inset(50%)}
    .saffron-identity :global(tr) {display:block;margin:1rem 0;border:1px solid #d8e1d0;border-radius:.5rem;overflow:hidden}
    .saffron-identity :global(th[scope="row"]) {display:block;border:0;padding:.85rem 1rem}
    .saffron-identity :global(td) {display:block;border:0;border-top:1px solid #d8e1d0;padding:.75rem 1rem}
    .saffron-identity :global(.mobile-label) {display:block;font-size:.9rem;color:#52634e;margin-bottom:.3rem}
  }

  .elderberry-material :global([id]) {scroll-margin-top:100px}
  .elderberry-material :global(nav ol) {flex-wrap:wrap}
  .elderberry-material :global(.prose) {max-width:820px;margin-inline:auto;overflow-wrap:anywhere;line-height:1.85}
  .elderberry-material :global(.elderberry-table) {width:100%;table-layout:fixed;border-collapse:collapse;margin:1.5rem 0}
  .elderberry-material :global(caption) {text-align:left;font-weight:600;color:#233e27;margin-bottom:.75rem}
  .elderberry-material :global(th), .elderberry-material :global(td) {padding:1rem;text-align:left;vertical-align:top;border:1px solid #d8e1d0;font-size:1rem;line-height:1.6}
  .elderberry-material :global(th) {background:#eff3e9;color:#233e27}
  .elderberry-material :global(.table-value) {white-space:nowrap}
  .elderberry-material :global(.mobile-label) {display:none}
  .elderberry-material :global(:focus-visible) {outline:3px solid #a77b16;outline-offset:3px}
  @media(max-width:600px) {
    .elderberry-material :global(caption) {display:block;width:100%}
    .elderberry-material :global(.elderberry-table), .elderberry-material :global(tbody) {display:block}
    .elderberry-material :global(thead) {position:absolute;width:1px;height:1px;overflow:hidden;clip-path:inset(50%)}
    .elderberry-material :global(tr) {display:block;margin:1rem 0;border:1px solid #d8e1d0;border-radius:.5rem;overflow:hidden}
    .elderberry-material :global(th[scope="row"]) {display:block;border:0;padding:.85rem 1rem}
    .elderberry-material :global(td) {display:block;border:0;border-top:1px solid #d8e1d0;padding:.75rem 1rem}
    .elderberry-material :global(.mobile-label) {display:block;font-size:.9rem;color:#52634e;margin-bottom:.3rem}
    .elderberry-material :global(.table-value) {font-weight:600}
  }

  .paprika-format :global([id]) {scroll-margin-top:100px}
  .paprika-format :global(nav ol) {flex-wrap:wrap}
  .paprika-format :global(.prose) {max-width:820px;margin-inline:auto;overflow-wrap:anywhere;line-height:1.85}
  .paprika-format :global(.paprika-choice) {padding:1.25rem;border:1px solid #d8e1d0;border-left:4px solid #537646;border-radius:.5rem;margin:1.5rem 0;background:#fafbf7}
  .paprika-format :global(.paprika-choice h3) {margin-top:0}
  .paprika-format :global(.paprika-choice p:last-child) {margin-bottom:0}
  .paprika-format :global(:focus-visible) {outline:3px solid #a77b16;outline-offset:3px}

  .sdil :global([id]) {scroll-margin-top:100px}
  .sdil :global(nav ol) {flex-wrap:wrap}
  .sdil :global(.prose) {max-width:820px;margin-inline:auto;overflow-wrap:anywhere;line-height:1.85}
  .sdil :global(.sdil-table) {overflow-x:auto;border:1px solid #d8e1d0;border-radius:.5rem;margin:1rem 0}
  .sdil :global(table) {min-width:660px;width:100%;table-layout:fixed;margin:0}
  .sdil :global(th),.sdil :global(td) {padding:1rem;vertical-align:top;border-bottom:1px solid #d8e1d0;font-size:.95rem;line-height:1.7}
  .sdil :global(th) {background:#eff3e9;color:#233e27}
  .sdil :global(.sdil-table-hint) {font-size:.85rem;color:#53644e}
  .sdil :global(:focus-visible) {outline:3px solid #a77b16;outline-offset:3px}

  .ginseng-heat :global([id]) {scroll-margin-top:100px}
  .ginseng-heat :global(nav ol) {flex-wrap:wrap}
  .ginseng-heat :global(.prose) {max-width:820px;margin-inline:auto;overflow-wrap:anywhere;line-height:1.85}
  .ginseng-heat :global(.ginseng-trial-table) {overflow-x:auto;isolation:isolate;border:1px solid #d8e1d0;border-radius:.5rem;margin:1rem 0}
  .ginseng-heat :global(table) {min-width:720px;width:100%;table-layout:fixed;border-collapse:separate;border-spacing:0;margin:0}
  .ginseng-heat :global(th),.ginseng-heat :global(td) {padding:1rem;vertical-align:top;border-bottom:1px solid #d8e1d0;font-size:.95rem;line-height:1.7}
  .ginseng-heat :global(th) {background:#eff3e9;color:#233e27}
  .ginseng-heat :global(td) {background:white}
  .ginseng-heat :global(th:first-child),.ginseng-heat :global(td:first-child) {position:sticky;left:0;z-index:1;width:32%;box-shadow:2px 0 #d8e1d0}
  .ginseng-heat :global(.ginseng-table-hint) {font-size:.85rem;color:#53644e}
  .ginseng-heat :global(:focus-visible) {outline:3px solid #a77b16;outline-offset:3px}

  .stevia-temporal :global([id]) {scroll-margin-top:100px}
  .stevia-temporal :global(nav ol) {flex-wrap:wrap}
  .stevia-temporal :global(.prose) {max-width:820px;margin-inline:auto;overflow-wrap:anywhere;line-height:1.85}
  @media(max-width:600px) {
    .ginseng-heat :global(table) {min-width:520px;width:520px}
    .ginseng-heat :global(th:first-child),.ginseng-heat :global(td:first-child) {width:130px}
    .ginseng-heat :global(th),.ginseng-heat :global(td) {padding:.75rem}
  }
  .citrus-material :global([id]) {scroll-margin-top:100px}
  .citrus-material :global(nav ol) {flex-wrap:wrap}
  .citrus-material :global(.prose) {max-width:820px;margin-inline:auto;overflow-wrap:anywhere;line-height:1.85}
  .citrus-material :global(.citrus-table) {width:100%;table-layout:fixed;border-collapse:collapse;margin:1.5rem 0}
  .citrus-material :global(caption) {text-align:left;font-weight:600;color:#233e27;margin-bottom:.75rem}
  .citrus-material :global(th), .citrus-material :global(td) {padding:1rem;text-align:left;vertical-align:top;border:1px solid #d8e1d0;font-size:1rem;line-height:1.6}
  .citrus-material :global(th) {background:#eff3e9;color:#233e27}
  .citrus-material :global(.mobile-label) {display:none}
  .citrus-material :global(:focus-visible) {outline:3px solid #a77b16;outline-offset:3px}
  @media(max-width:600px) {
    .citrus-material :global(caption) {display:block;width:100%}
    .citrus-material :global(.citrus-table), .citrus-material :global(tbody) {display:block}
    .citrus-material :global(thead) {position:absolute;width:1px;height:1px;overflow:hidden;clip-path:inset(50%)}
    .citrus-material :global(tr) {display:block;margin:1rem 0;border:1px solid #d8e1d0;border-radius:.5rem;overflow:hidden}
    .citrus-material :global(th[scope="row"]) {display:block;border:0;padding:.85rem 1rem}
    .citrus-material :global(td) {display:block;border:0;border-top:1px solid #d8e1d0;padding:.75rem 1rem}
    .citrus-material :global(.mobile-label) {display:block;font-size:.9rem;color:#52634e;margin-bottom:.3rem}
  }


  .cranberry-pac :global([id]) {scroll-margin-top:100px}
  .cranberry-pac :global(nav ol) {flex-wrap:wrap}
  .cranberry-pac :global(.prose) {max-width:820px;margin-inline:auto;overflow-wrap:anywhere;line-height:1.85}
  .cranberry-pac :global(.cranberry-table) {width:100%;table-layout:fixed;border-collapse:collapse;margin:1.5rem 0}
  .cranberry-pac :global(caption) {text-align:left;font-weight:600;color:#233e27;margin-bottom:.75rem}
  .cranberry-pac :global(th), .cranberry-pac :global(td) {padding:1rem;text-align:left;vertical-align:top;border:1px solid #d8e1d0;font-size:1rem;line-height:1.6}
  .cranberry-pac :global(th) {background:#eff3e9;color:#233e27}
  .cranberry-pac :global(.mobile-label) {display:none}
  .cranberry-pac :global(:focus-visible) {outline:3px solid #a77b16;outline-offset:3px}
  @media(max-width:600px) {
    .cranberry-pac :global(caption) {display:block;width:100%}
    .cranberry-pac :global(.cranberry-table), .cranberry-pac :global(tbody) {display:block}
    .cranberry-pac :global(thead) {position:absolute;width:1px;height:1px;overflow:hidden;clip-path:inset(50%)}
    .cranberry-pac :global(tr) {display:block;margin:1rem 0;border:1px solid #d8e1d0;border-radius:.5rem;overflow:hidden}
    .cranberry-pac :global(th[scope="row"]) {display:block;border:0;padding:.85rem 1rem}
    .cranberry-pac :global(td) {display:block;border:0;border-top:1px solid #d8e1d0;padding:.75rem 1rem}
    .cranberry-pac :global(.mobile-label) {display:block;font-size:.9rem;color:#52634e;margin-bottom:.3rem}
  }
  .cranberry-pac :global(.table-value) {white-space:nowrap}

  .vanilla-material :global([id]) {scroll-margin-top:100px}
  .vanilla-material :global(nav ol) {flex-wrap:wrap}
  .vanilla-material :global(.prose) {max-width:820px;margin-inline:auto;overflow-wrap:anywhere;line-height:1.85}
  .vanilla-material :global(.vanilla-table) {width:100%;table-layout:fixed;border-collapse:collapse;margin:1.5rem 0}
  .vanilla-material :global(caption) {text-align:left;font-weight:600;color:#233e27;margin-bottom:.75rem}
  .vanilla-material :global(th), .vanilla-material :global(td) {padding:1rem;text-align:left;vertical-align:top;border:1px solid #d8e1d0;font-size:1rem;line-height:1.6}
  .vanilla-material :global(th) {background:#eff3e9;color:#233e27}
  .vanilla-material :global(.mobile-label) {display:none}
  .vanilla-material :global(:focus-visible) {outline:3px solid #a77b16;outline-offset:3px}
  @media(max-width:600px) {
    .vanilla-material :global(caption) {display:block;width:100%}
    .vanilla-material :global(.vanilla-table), .vanilla-material :global(tbody) {display:block}
    .vanilla-material :global(thead) {position:absolute;width:1px;height:1px;overflow:hidden;clip-path:inset(50%)}
    .vanilla-material :global(tr) {display:block;margin:1rem 0;border:1px solid #d8e1d0;border-radius:.5rem;overflow:hidden}
    .vanilla-material :global(th[scope="row"]) {display:block;border:0;padding:.85rem 1rem}
    .vanilla-material :global(td) {display:block;border:0;border-top:1px solid #d8e1d0;padding:.75rem 1rem}
    .vanilla-material :global(.mobile-label) {display:block;font-size:.9rem;color:#52634e;margin-bottom:.3rem}
  }

</style>
